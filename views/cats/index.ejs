<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>====^_^====</title>
</head>
<body>
    <h1>Look at all the cuties!</h1>
    <ul>
      <% cats.forEach(cat => { %>
        <li><%= cat.name %>
            <br>
           <img src="<%= cat.image %>" alt="<%= cat.name %> Image">
        </li>
      <% }); %>
    </ul>
</body>
</html>