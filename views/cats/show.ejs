<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= cat.name %></title>
</head>
<body>
    <h1><%= cat.name %></h1>
    <% if (cat.gender === "Other") {%>
        <h2>They are a good cat!</h2>      
        <%} else{%>
        <h2>They are a good <%= toLowerCase(cat.gender) %>!</h2>
    <% } %>        
    <img src="<%= cat.image %>" alt="<%= cat.name %> picture">
    <h2>Age <%= cat.age %></h2>
   
</body>
</html>